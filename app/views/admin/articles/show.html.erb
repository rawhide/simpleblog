<table class="table table-hover">
  <tbody>
    <tr>
      <th>タイトル</th>
      <td><%= @article.title %></td>
    </tr>
    <tr>
      <th>本文</th>
      <td><%= raw @article.body %></td>
    </tr>
  </tbody>
</table>
<%= link_to '戻る', admin_articles_path, class: "btn btn-default" %> <%= link_to '編集', edit_admin_article_path(@article), class: "btn btn-primary" %>
