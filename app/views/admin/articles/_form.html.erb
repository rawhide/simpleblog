<%= form_for(@artile) do |f| %>
  <% if @artile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@artile.errors.count, "error") %> prohibited this artile from being saved:</h2>

      <ul>
      <% @artile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
    <%= f.label :pv %><br>
    <%= f.text_field :pv %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
