<%= form_for @article, url: "#" do |f| %>
  <fieldset>
    <legend>記事登録</legend>

    <%= render "form", f: f, article: @article %>

    <div class="text-center">
      <%= f.submit "登録", class: "btn btn-default" %>
    </div>
  </fieldset>
<% end %>
